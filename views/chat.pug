extends layout

block content
  h1= title
  p Welcome to #{title}
  ul.pages
    li.chat.page
        div.chatArea
            ul.messages
        input.inputMessage(placeholder="Type here...")
    li.login.page
        div.form
            h3.title What is your nickname?
            input.usernameInput(type='text' maxlenth='20')
                