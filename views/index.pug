extends layout

block content
  h1= title
  p Welcome to #{title}

  input(type='text').class-input#name

  - var condition = true
  if condition
    p It's true
    
  - var anyArray = [1,2,3]
  each value in anyArray
    p= value